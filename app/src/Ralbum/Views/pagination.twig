{% if pagination.getNumberOfPages > 1 %}
	<ul id="pagination">
		{% if pagination.currentPage != 1 %}
			<li>
				<a href="{{ pagination.getBaseUrl }}{% if '?' in pagination.getBaseUrl %}&amp;{% else %}?{% endif %}page={{ pagination.currentPage-1 }}">&larr; 上一页</a>
			</li>
		{% else %}
			<li class="inactive">
				<a href="#">&larr; 上一页</a>
			</li>
		{% endif %}
		{% if pagination.currentPage != pagination.getNumberOfPages %}
			<li class="next">
				<a href="{{ pagination.getBaseUrl }}{% if '?' in pagination.getBaseUrl %}&amp;{% else %}?{% endif %}page={{ pagination.currentPage+1 }}">下一页 &rarr;</a>
			</li>
		{% else %}
			<li class="next inactive">
				<a href="#">下一页 &rarr;</a>
			</li>
		{% endif %}

	</ul>
{% endif %}
